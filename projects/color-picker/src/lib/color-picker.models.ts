export interface Point {
    x: number;
    y: number;
}

export interface RGBA {
    r: number,
    g: number,
    b: number,
    a: number
}

export enum ColorTypes {
    HEX = 'hex',
    RGB = 'rgb'
}